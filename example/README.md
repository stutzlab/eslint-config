# example

Runnable consumer example and integration test for `@stutzlab/eslint-config`.

Installs the locally built `.tgz` pack from `lib/dist2/` and verifies that the ESLint rules apply correctly.

## Development

```bash
make build
make test
```
